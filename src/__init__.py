# HelixNet application version -- the SINGLE source of truth for the build stamp
# shown in the POS status bar. Bump on a meaningful release (semantic-release can
# automate this later; the [tool.semantic_release] config already points here).
__version__ = "3.3.0"

runtime:
  mode: production
  log_level: info

services:
  redis_url: redis://redis:6379
  postgres_url: postgres://helix_user:helix_pass@postgres:5432/helix_db
  broker_url: amqp:/helix_user:helix_pass@rabbitmq:5672//

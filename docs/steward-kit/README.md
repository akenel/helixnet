# Moved → Ground Control repo

The Steward's Kit graduated into its own standalone repo: **Ground Control**
(*"Mission control for your AI copilot"*).

- Local: `~/repos/ground-control`
- The kit files (CLAUDE template, standing rules, memory system, worked example)
  now live there, in `kit/`.

This folder is kept only as a pointer so the trail isn't lost. Do new work in
the Ground Control repo, not here.

# HANDOFF — Banco, end of 2026-07-17

**For the next session (post-compaction). Read this first.** A monster hypercare day: workbench build +
live bug-hunting + language cleanup + timesheet weather + honest costing. Everything below is FACT-checked
against the running system at handoff, not memory.

---

## STATE — everything is shipped and in parity

- **main = prod = sandbox = `977f33b`.** No pending deploys, no uncommitted app code.
- **39 commits** shipped today (prod baseline was `4a719d6`). Every one went through the gate ladder
  (`make deploy ENV=…` from the laptop → app-gate + login-audit) and each prod deploy was **backup-verified**
  (restore-checked `59/5200/186`).
- Prod: `banco.lapiazza.app` (CHF, Artemis Lucerne). Sandbox: `sandbox-banco.lapiazza.app` (**EUR**,
  Artemis Roma — this matters: it's why the currency bugs surfaced).

## WHAT SHIPPED TODAY (all on prod, all verified)
- **The Migration Workbench** (BL-131): export a shelf → fill the .xlsx at the shelf (scan barcodes into
  the cell — the gun is a keyboard) → import with dry-run preview → apply. Pulls image + title + price
  from a pasted page URL (og:image / JSON-LD); reads the page BODY for the description; reads TEXT pasted
  into Notes (amazon 503s our fetch, a human doesn't). Guards: cross-wire refusal (a barcode owned by
  another product is refused, not re-pointed), shared-photo refusal (one URL on two rows), category
  dropdown = canonical only, dry-run by default.
- **BL-132** brand registry (35 verified official sites → brand-first + `site:` image search).
- **BL-133** Zippo lane (scan a rack, the barcode looks itself up — mainstream stock only).
- **BL-135** ASK-OUR-OWN-CATALOG-FIRST (10,284 FourTwenty reference rows were unused; "We already have"
  column on the sheet). Only ~10% strong auto-fill because the seeded names are ENGLISH and the reference
  is GERMAN — barcode match is the real unlock (language-agnostic).
- **BL-129** clean scanned barcodes (gun junk was making KNOWN codes read as unknown → duplicates).
- **BL-130 / 134** category funnel: photo-read + FourTwenty headings mapped; "Papers & Filters" was
  silently mis-filing 772 filters as papers; dabbers/scales/lighters/stash rescued from the catch-all.
- **BL-042** graceful over-long-field 422 (never a 500 on a fat-finger).
- **BL-043** postcard shows the gallery photo when cover is NULL (the Muffin 📦).
- **BL-045** search floats real name matches to the top under ANY sort ("muff" was buried under fuzzy junk).
- **BL-046** the taxman startup-seed 500 (never insert keycloak_id=NULL).
- **BL-047** auto-weather on the timesheet (Open-Meteo, free/keyless, from the shop's city) + Settings note.
- **BL-047b** the COST EYEBALL: markup pull-down (ABC per-class defaults: tobacco 10 / CBD 45 / café 60 /
  standard 50), `store_settings.default_markup_pct` (manager-editable, in Settings→Discounts), always
  flagged "≈ estimated — verify against a slip". Costs stay blank until a manager fills one.
- **BL-141** click-photo-to-zoom + Details/Postcard links on the bench card.
- **BL-142** paste-a-link photo + Sells-for price field + dabber section.
- **BL-144 (partial)** postcard 🏠 back-to-kiosk header link (the safe nav slice).
- **Language mess CLOSED on prod**: 86 German descriptions were tagged `source_lang='en'` (import lie) →
  corrected to 'de' + re-translated; EN/FR/IT tabs now serve the right language. Re-audit: 0 remaining.
- **/doctor cleanup**: auto mode on; CLAUDE.md trimmed ~3,600 tokens/session (print/postcard/SOP → the
  `print-postcards` skill). ⚠ These two edits are UNCOMMITTED working-tree changes (see loose ends).

---

## ⏳ STILL TO CHECK (human-green — machine-green isn't human-green)

Everything is machine-verified, but Angel's own rule needs a human pass. Priority order:

1. **Workbench round-trip in SANDBOX** — `docs/testing/banco/WORKBENCH-UAT-V2.html` (13 checks, ~20 min).
   Angel got ~9/34 of the long version before falling asleep. The **§5 "try to break it"** guards
   (cross-wire refusal, shared-photo refusal, junk-category rejection) are the highest-value unproven part —
   they write to the catalog. Do this on SANDBOX.
2. **Cost eyeball** (BL-047b, just shipped prod) — pick a markup on a priced Cockpit card, confirm the cost
   fills + the amber "≈ estimated" note shows; set a shop default in Settings→Discounts and confirm it
   drives the pull-down.
3. **Timesheet + weather** — `docs/testing/banco/MYDAY-TIMESHEET-TESTSHEET.html`. Angel sandbox-tested My
   Day as Pam ("sweet"); the **AI end-of-day note quality (§3)** and the auto-weather line are worth a real
   read on a live day.
4. **The 86 re-translated descriptions** — spot-check a few EN/FR/IT tabs; the LLM translates on first view
   per language, so the first hit is slow then cached. Detector is conservative (clear German only).

## 📋 STILL TO DO (booked specs — not built)

- **BL-143 cost-factor (the RICHER layer)** — `docs/SPEC-BL-143-cost-factor.md`. BL-047b did the MVP
  (settings default + ABC per-class). The rest: **per-SUPPLIER margins** (Sylvie 30%, FourTwenty ~55%, +
  EUR→CHF FX for Near Dark), and a permanent **`cost_source: estimated|manual|slip`** stamp on the row
  (currently the estimate is only flagged in the UI, not stored). 🔴 WARNING in the spec: 199
  reference_products already sit at a fake 33.3% margin — a formula stored as if real. Also: the bench
  "still to do" counter is 92% just-missing-COST (4,818 products are otherwise complete) — splitting cost
  into its own Receiving/slip metric would turn "5,167 to do" into "~380", a morale fix.
- **BL-144 postcard nav (the rest)** — `docs/SPEC-BL-144-postcard-nav.md`. Staff-login + member-self-service
  deep-links. Needs auth decisions (a PUBLIC share page must only deep-link to the real login, expose
  nothing). The 🏠 back-link is already shipped.
- **194 wall-of-text descriptions** — the reformat-to-bullets job timed out at 5/199. New descriptions are
  clean (prompt fixed + pre-wrap rendering); the existing 194 need a proper BACKGROUND job (not a script to
  babysit — 40 LLM calls/request times out).
- **SUPPLIER-CATALOG re-import** — FourTwenty publishes a live dropship feed (3 CSVs: products / stock /
  **specifications** = 155k spec rows we never imported = the missing descriptions + brands + real
  categories). We imported ~1/3, partially. This is the big catalog unlock. (memory
  `banco-supplier-catalog-enrichment-strategy`.) Tamar is not imported at all.
- **Bulk Intake front door** — the workbench proves the round-trip; a "drop a folder of photos / a CSV / a
  supplier URL → review board → create" screen would make it a shop-owned tool, not a dev script.

## 🧹 LOOSE ENDS

- **UNCOMMITTED working-tree edits** (from /doctor, harmless docs — NOT app code):
  `M CLAUDE.md` (trimmed) + `?? .claude/skills/` (the new print-postcards skill). Decide with Angel:
  commit them or `git checkout CLAUDE.md` + `rm -rf .claude/skills/print-postcards`. They don't affect the app.
- **BL-100 seal-inspection sweep owed**: the `42425700`→Tycoon cross-wire was ONE of the 420 internal
  `42xxxxxx` codes; a read-only sweep for other mis-assigned short 42-prefix aliases is owed (confirm each
  physically, never guess on prod). memory `banco-product-matching-integrity-bl100`.

## 🧭 KEY CONTEXT (don't relearn the hard way)
- **THE ONE REAL USER** is Felix's new junior hire — she does the full manual inventory as onboarding.
  Design every tool FOR HER (brand-new, no barcode xp). memory `banco-first-real-user-inventory-ownership`.
  Felix + Rafi have prod accounts but Angel expects little feedback from them — he drives it.
- **SANDBOX-FIRST**: default to sandbox for all work incl. data. Prod only on Angel's explicit push. Found a
  real prod-data bug? SURFACE it, don't fix live. (I broke this once today early — the GIZEH item — and got
  corrected.) memory `feedback-sandbox-first-never-touch-prod`.
- **The `text()` import trap**: `sqlalchemy.text` is NOT imported at module scope in pos_router — any new
  helper using `text()` needs a local `from sqlalchemy import text` or it 500s. Bit us TWICE today (BL-135,
  BL-043). Check this on every new DB helper.
- **Currency**: never hardcode CHF. Store carries `currency`; the client's `formatPrice`/`_cfg` are the one
  seam. Hardcoded CHF bit us in FIVE places today (BL-136 a/b/c).
- **Deploy = restart, NOT rebuild**: `deploy-banco.py` restarts the container; a NEW pip dependency
  (openpyxl) or DB column needs a manual step. openpyxl was pip-installed into the containers at runtime +
  baked into requirements; new deps repeat this.
- **make test**: 2021 pass / **3 known-flaky** (camper doc-upload mock + 2 LLM) — NOT deploy blockers.
- **TESTSHEET / TIMESHEET code words** are in CLAUDE.md. "ON DECK" = read docs/BANCO-WORKLIST.md.

## THE STANDING RULE THAT EARNED ITS KEEP TODAY
"If one seal fails, check all the seals." Every bug today had siblings: currency (5 places), the label-gate
on image upload (4 upload paths), the text-import trap (twice). When you find one, sweep for the class.

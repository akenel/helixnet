# Banco Intro — Shot List / Run-Sheet (record on SANDBOX)

*Pairs with the teleprompter `BANCO-INTRO-ZERO-INVENTORY-SCRIPT.html` (VO on the left). This is
the **what-to-click + when**. ~3 min total.*

---

## Setup (before you hit record)
- **Environment: SANDBOX** → `https://sandbox-banco.lapiazza.app/pos`
  The login now shows a **purple `SANDBOX` badge** (env-colour is live) — so you, and the viewer,
  always know it's the safe trial box.
- **Reset to empty:** `ssh root@46.62.138.218 'cd /opt/helixnet && make sandbox-reset'`
- **Seed 3–4 items** so it looks like a *real young shop*, not barren (a blank catalogue reads as
  "broken"). Either add a few live (papers, lighter, grinder, a CBD oil — 2 min, authentic), or run
  the velocity seed for a fuller shop. **(Say the word and I'll seed 4 for you.)**
- **Login: `pam` / `helix_pass`** (cashier — she does the whole flow).
- **Browser:** ~**90% zoom**, narrow / mobile-ish width, **window stationary** (don't move it — crops
  clean afterwards). **DND on**, no notifications. **Raw file, not Telegram.**
- Teleprompter open on the left; screen-record the browser on the right.
- **Pick the "new item"** you'll create to be something **NOT** in your seed (e.g. the €40 cream, or a
  Tamar hot-seller) — so the search genuinely comes up empty.

---

## The shots

| # | Beat (VO) | On screen | Your action | ~time | Captured as |
|---|-----------|-----------|-------------|-------|-------------|
| 0 | **Cold open** — "Stop counting" | the purple **SANDBOX login** | just talk over it | 0:15 | card + VO (login backdrop) |
| 1 | **The problem** | login → dashboard | **log in as pam** | 0:15 | card + VO |
| 2 | **Why it's a lie** | open **Catalog** (the few items) | click Catalog | 0:20 | card + VO; show the small catalogue |
| 3 | **The reframe** — "So… don't" | dashboard / catalogue | talk | 0:12 | card + VO |
| 4 | **Born once** | **New Sale** | click New Sale; hover the search | 0:25 | app + VO |
| 5 | **▶ LIVE INSERT** | New Sale | **the clicks below** | 0:35 | **app clicks + VO — the money shot** |
| 6 | **The contrast** | back to dashboard / a report | talk | 0:25 | card + VO |
| 7 | **Close** | Catalog — now **+1 item** | talk | 0:15 | card + VO |
| 8 | **Outro** | — | — | — | card only (post) |

**Beat 5 — the live insert, click for click (do it slowly + clearly):**
1. **New Sale** → search bar → type the new item's name (the one NOT seeded).
2. **No results** → tap **"Item not on file / New item."**
3. Type the **name** → **price** → **📷 photo** → **Save.**
4. It's in the cart / catalogue — real, named, priced, pictured.
5. **Search the same name again** → it's **instant**, found, photo + price all there.

*(Say the line "…and I never counted a thing" as step 5 lands.)*

---

## Notes
- **You don't need slides.** The title cards (the conceptual beats) get **added in post** (I render
  them with Puppeteer and overlay them). So just talk over the app as a calm backdrop; I drop the
  cards on top of beats 0–3, 6, 8.
- **The only beats you MUST screen-record cleanly are 4–5** (the app action). Everything else can be
  VO-over-card. Running the app continuously as a backdrop is fine and easiest.
- **Stationary window = easy crop.** Since you never move it, I can crop to the phone-ish frame in
  one pass.
- This is the **pilot** — get the live insert (beat 5) clean and the rest follows.
